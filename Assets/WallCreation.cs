using UnityEngine;
using System.Collections;

public class WallCreation : MonoBehaviour {

	// Use this for initialization


	void Start () {
		//GameObject myPassage = (GameObject)Instantiate(Resources.Load("Passage"), new Vector3 (, 5, 5), Quaternion.identity);
		//myPassage.AddComponent<Rigidbody> ();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
